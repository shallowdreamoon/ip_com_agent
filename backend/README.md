# Backend Scaffold (FastAPI)

This folder is prepared for future FastAPI-based backend implementation.

- `api/`: API route definitions
- `agents/`: multi-agent orchestration logic (placeholder)
- `services/`: domain services
- `models/`: data models
- `data/`: local data assets
- `reports/`: generated report outputs

No real agent logic is implemented in this step.
